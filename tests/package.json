{
  "name": "@repo/tests",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:tenancy": "vitest run tenancy"
  },
  "dependencies": {
    "@repo/database": "workspace:*",
    "@repo/permissions": "workspace:*",
    "@repo/tenancy": "workspace:*"
  },
  "devDependencies": {
    "better-sqlite3": "^11.7.0",
    "drizzle-orm": "^0.45.2",
    "typescript": "^5",
    "vitest": "^3.2.0",
    "@types/better-sqlite3": "^7.6.12"
  }
}
